# Orchestrator

## About
This is a monorepo and contains two modules - `pmt-svc` and `simple-kafka-order`. The two applications
interact using kafka.

## Run
Please use the command below in your terminal to bring up the project post cloning
`docker-compose up --build -d`